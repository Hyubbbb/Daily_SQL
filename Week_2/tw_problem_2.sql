-- 과일로 만든 아이스크림 고르기(programmers-SELECT)

SELECT f.FLAVOR
FROM FIRST_HALF f LEFT JOIN ICECREAM_INFO i ON f.FLAVOR = i.FLAVOR
WHERE TOTAL_ORDER > 3000 AND INGREDIENT_TYPE = 'fruit_based'
ORDER BY f.TOTAL_ORDER DESC
