SELECT 
    ID, 
    CASE 
    WHEN SIZE_OF_COLONY > 1000
        THEN 'HIGH'
    WHEN 100 < SIZE_OF_COLONY AND SIZE_OF_COLONY <= 1000
        THEN 'MEDIUM'
    -- # WHEN SIZE_OF_COLONY <= 100
    -- #     THEN 'LOW'
    ELSE 'LOW'
    END AS SIZE
FROM ECOLI_DATA
ORDER BY ID
