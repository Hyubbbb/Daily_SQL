-- 작동순서 : FROM - ON - JOIN - WHERE - 

SELECT COUNT(*) AS FISH_COUNT, N.FISH_NAME
FROM FISH_NAME_INFO AS N
INNER JOIN FISH_INFO AS I
    ON N.FISH_TYPE = I.FISH_TYPE
GROUP BY N.FISH_NAME
ORDER BY FISH_COUNT DESC;
