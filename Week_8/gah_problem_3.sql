-- [PROGRAMMERS] GROUP BY > 성분으로 구분한 아이스크림 총 주문량

SELECT i.INGREDIENT_TYPE, SUM(f.TOTAL_ORDER) TOTAL_ORDER
FROM FIRST_HALF f, ICECREAM_INFO i
WHERE f.FLAVOR = i.FLAVOR
GROUP BY i.INGREDIENT_TYPE
ORDER BY 2;
