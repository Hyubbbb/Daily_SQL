-- 코드를 입력하세요
WITH RECURSIVE HOUR AS(
    SELECT 0 AS H
UNION ALL
    SELECT H + 1 
    FROM HOUR
    WHERE H < 23
)

SELECT H AS HOUR, COUNT(HOUR(B.DATETIME)) AS COUNT
FROM HOUR A LEFT JOIN ANIMAL_OUTS B
ON A.H = HOUR(B.DATETIME)
GROUP BY HOUR
ORDER BY HOUR;
