-- 코드를 입력하세요
SELECT PRODUCT_CODE, P.PRICE*S.TOTAL_SALES SALES
FROM PRODUCT P
JOIN (SELECT PRODUCT_ID, SUM(SALES_AMOUNT) TOTAL_SALES
      FROM OFFLINE_SALE
      GROUP BY PRODUCT_ID) S
    ON P.PRODUCT_ID = S.PRODUCT_ID
GROUP BY PRODUCT_CODE
ORDER BY SALES DESC, PRODUCT_CODE
