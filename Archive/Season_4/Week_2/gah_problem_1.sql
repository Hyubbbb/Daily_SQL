-- [PROGRAMMERS] GROUP BY > 부서별 평균 연봉 조회하기

SELECT E.DEPT_ID, D.DEPT_NAME_EN, E.AVG_SAL
FROM HR_DEPARTMENT D JOIN
    (SELECT DEPT_ID, ROUND(AVG(SAL)) AVG_SAL
     FROM HR_EMPLOYEES
     GROUP BY DEPT_ID) E
    ON D.DEPT_ID = E.DEPT_ID
ORDER BY E.AVG_SAL DESC
