SELECT U2.USER_ID,NICKNAME,SUM(U1.PRICE) AS TOTAL_SALES
FROM USED_GOODS_BOARD AS U1
INNER JOIN USED_GOODS_USER AS U2 ON U1.WRITER_ID=U2.USER_ID
WHERE STATUS ="DONE"
GROUP BY U2.USER_ID
HAVING SUM(U1.PRICE) >=700000
ORDER BY TOTAL_SALES
