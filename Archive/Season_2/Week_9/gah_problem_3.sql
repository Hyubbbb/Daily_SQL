-- [PROGRAMMERS] SELECT > 조건에 맞는 개발자 찾기

SELECT DISTINCT(d.ID), d.EMAIL, d.FIRST_NAME, d.LAST_NAME
FROM SKILLCODES s JOIN DEVELOPERS d
     ON s.CODE & d.SKILL_CODE
WHERE s.NAME = 'C#' OR s.NAME = 'Python'
ORDER BY 1
