SELECT 
    -- 날짜에서 연도만 추출
    EXTRACT(YEAR FROM DIFFERENTIATION_DATE) AS YEAR,
    -- 연도별 최대 크기에서 각 대장균의 크기를 빼서 편차 계산
    MAX(SIZE_OF_COLONY) OVER (PARTITION BY EXTRACT(YEAR FROM DIFFERENTIATION_DATE)) - SIZE_OF_COLONY AS YEAR_DEV, ID
FROM ECOLI_DATA
ORDER BY YEAR ASC, YEAR_DEV ASC;